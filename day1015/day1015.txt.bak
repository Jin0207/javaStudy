<상속관계의 메서드, 객체참조>

상속관계에 있을 때,
부모클래스의 참조변수가 자식클래스객체를 참조할 수 있다.
만약,
자식클래스가 메서드를 오버라이딩 했다면,
부모클래스의 참조변수가 가리키는 객체의 메서드 또한, 오버라이딩된 메서드이다.

만약,
자식클래스만이 가지고 있는 메서드라면,
부모클래스의 참조변수는 이를 참조할 수 없다 --> 호출할 수 없다.
호출하고 싶다면 참조변수를 자식클래스형으로 형변환 시켜주어야한다.

<추상클래스와 추상메서드>
추상메서드는
선언부만을 가지고 있고 구현부(body)는 가지고 있지않다.
상속을 받게되는 자식클래스들이 만드시 가지고 있어야할 메서드를 추상메서드로 만든다.

추상클래스는
한 개 이상의 추상메서드를 가지고 있는 클래스로
class 앞에 "abstract"키워드를 붙혀 추상클래스임을 정의해야한다.
추상클래스형으로는 객체를 생성할 수 없다.
단,
추상클래스형 참조변수가 (추상메서드를 오버라이딩한) 자식 객체를 참조할 수 는 있다.

상속을 통해서 추상클래스를 부모로 가지고 있는
자식클래스는 반드시! 추상메서드를 오버라이딩하여 구현부를 작성해주어야한다.

----------------------------------------------------------------------------
<"다형성">
메서드의 오버라이딩으로 인하여
한 개의 이름으로 정의된 메서드들이 클래스마다 다른 기능을 한다.
--객체마다 하는 일이 다르다.
